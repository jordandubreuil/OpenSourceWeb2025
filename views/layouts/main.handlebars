<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body>
    {{#if success_msg}}<p>{{success_msg}}</p>{{/if}}
    {{#if error_msg}}<p>{{error_msg}}</p>{{/if}}
    {{#if error}}<p>{{error}}</p>{{/if}}

    {{{body}}}
</body>
</html>